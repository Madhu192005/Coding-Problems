IP: aabbbeeeffgg     OP:a2b3e3f2g2

for(int i=0;i<length();i++){
if(i<s.length()-1 && s.charAt(i)==s.charAt(i+1)){
c++;
}else{
System.out.print(s.charAt(i));
System.out.print(c);
c=1; //reset counter
}
}
