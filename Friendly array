class Solution {
    public int calculateFriendliness(int[] arr) {
        // code here
        int sum=0;
        int n=arr.length;
        for(int i=0;i<n-1;i++){
            sum+=Math.abs(arr[i]-arr[i+1]);
        }
        sum+=Math.abs(arr[n-1]-arr[0]);
        return sum;
    }
}
